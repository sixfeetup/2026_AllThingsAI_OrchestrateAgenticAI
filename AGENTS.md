# demo-init Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-11

## Active Technologies

- Shell scripts (`zsh`/POSIX bash compatible where practical), Python 3 via `uv` for helper automation, Markdown/HTML for runbooks and explainers + `uv`, Codex CLI, Ralph CLI, Make, Postgres, repo-local markdown and HTML artifacts (001-conference-demo)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Shell scripts (`zsh`/POSIX bash compatible where practical), Python 3 via `uv` for helper automation, Markdown/HTML for runbooks and explainers: Follow standard conventions

## Recent Changes

- 001-conference-demo: Added Shell scripts (`zsh`/POSIX bash compatible where practical), Python 3 via `uv` for helper automation, Markdown/HTML for runbooks and explainers + `uv`, Codex CLI, Ralph CLI, Make, Postgres, repo-local markdown and HTML artifacts

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
