[project]
name = "document-review-demo"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "pymupdf",
    "chromadb",
    "sentence-transformers",
    "mcp",
    "fpdf2",
    "path",
    "pyyaml",
    "python-docx",
    "openpyxl",
]
