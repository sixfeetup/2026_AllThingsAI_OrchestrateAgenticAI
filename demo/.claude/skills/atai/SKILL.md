# atai

Alias for `/atai-demo`. Route all arguments directly to that skill.

## Triggers

- `/atai` — same as `/atai-demo`
- `/atai <args>` — same as `/atai-demo <args>`

## Instructions

Invoke the `atai-demo` skill with whatever arguments were passed. Behave identically.
